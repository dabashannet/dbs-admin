<?php

use Illuminate\Support\Facades\Route;
use Plugins\{{ pluginStudly }}\Http\Controllers\{{ pluginStudly }}Controller;

Route::prefix('plugin/{{ pluginName }}/api')
    ->middleware('api')
    ->group(function () {
        Route::get('/', [{{ pluginStudly }}Controller::class, 'index']);
    });
